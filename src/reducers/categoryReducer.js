import buttonArray from "../data/buttonArray";

let initialState = buttonArray;

const CategoryPruducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default CategoryPruducer;
